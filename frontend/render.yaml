# frontend/render.yaml
services:
  - type: web
    name: e-mart-frontend
    env: node
    buildCommand: npm ci && npm run build
    startCommand: npx serve -s dist -l $PORT
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_API_BASE_URL
        sync: false
